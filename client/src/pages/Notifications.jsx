import { useEffect, useState } from 'react'; import api from '../lib/api';
export default function Notifications(){const [items,setItems]=useState([]);const load=()=>api.get('/notifications').then(r=>setItems(r.data.items));useEffect(()=>{load()},[]);const read=async id=>{await api.patch(`/notifications/${id}/read`);load()};return <div><h1 className="text-3xl font-black">Notifications</h1><div className="mt-6 space-y-3">{items.map(n=><button key={n._id} onClick={()=>read(n._id)} className={`card block w-full p-4 text-left ${n.isRead?'opacity-60':''}`}><div className="font-bold">{n.title}</div><div className="mt-1 text-sm text-slate-600">{n.message}</div></button>)}{!items.length&&<div className="card p-8 text-center text-slate-500">No notifications yet.</div>}</div></div>}
