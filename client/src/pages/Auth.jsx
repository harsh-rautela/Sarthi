import { useState } from 'react'; import { Link, useNavigate } from 'react-router-dom'; import { useAuth } from '../lib/AuthContext';
export default function Auth({mode='login'}){const isLogin=mode==='login'; const [form,setForm]=useState({name:'',email:'',password:''}); const [error,setError]=useState(''); const [busy,setBusy]=useState(false); const {login,register}=useAuth(); const nav=useNavigate();
 const submit=async e=>{e.preventDefault();setBusy(true);setError('');try{if(isLogin)await login({email:form.email,password:form.password});else await register(form);nav('/dashboard')}catch(e){setError(e.response?.data?.message||'Request failed')}finally{setBusy(false)}};
 return <div className="grid min-h-screen place-items-center bg-slate-50 px-4"><form onSubmit={submit} className="card w-full max-w-md p-8"><Link to="/" className="font-bold text-emerald-800">SchemeSathi</Link><h1 className="mt-6 text-3xl font-black text-slate-900">{isLogin?'Welcome back':'Create your account'}</h1><p className="mt-2 text-sm text-slate-500">{isLogin?'Use your citizen account to continue.':'Create a profile for personalized recommendations.'}</p>{error&&<div className="mt-4 rounded-xl bg-red-50 p-3 text-sm text-red-700">{error}</div>}<div className="mt-6 space-y-4">{!isLogin&&<label className="block"><span className="label">Name</span><input className="input mt-1" required value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label>}<label className="block"><span className="label">Email</span><input type="email" className="input mt-1" required value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label className="block"><span className="label">Password</span><input type="password" minLength="6" className="input mt-1" required value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></label></div><button disabled={busy} className="btn-primary mt-6 w-full">{busy?'Please wait…':isLogin?'Login':'Register'}</button><p className="mt-5 text-center text-sm text-slate-600">{isLogin?'New here? ':'Already registered? '}<Link className="font-semibold text-emerald-700" to={isLogin?'/register':'/login'}>{isLogin?'Create account':'Login'}</Link></p>{isLogin&&<div className="mt-5 rounded-xl bg-emerald-50 p-3 text-xs text-emerald-900">Seeded demo: demo@schemesathi.local / Demo@123</div>}</form></div>}
